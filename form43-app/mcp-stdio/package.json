{
  "name": "form43-mcp",
  "version": "0.1.0",
  "description": "Stdio MCP adapter for form43-app. Proxies JSON-RPC to the remote Streamable HTTP /mcp endpoint, so Claude Desktop and other local MCP clients can drive the Cloudflare Worker.",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "form43-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0"
  }
}
